# 卒研: oneway違反7件（コードB）の誤マッチ原因調査

## 背景・現状

前回の調査（`investigation_138533178.md`）で一覧化した oneway 違反18件について、ユーザーがストリートビューで現地確認を行った。その結果、以下の分類が得られた。

| コード | 意味 | 件数 | 該当 way_id |
|---|---|---|---|
| A | 現地に「自転車を除く」補助標識あり（OSMデータ不備） | 6 | 138533178, 23690216, 142222817, 80835360, 350353685, 325347768 |
| **B** | **左側通行していれば合法。システムが逆方向の道を選択している疑い** | **7** | **22961575, 741785139, 667962675, 474601303, 271979254, 1429406683, 263457845** |
| C | 真の一方通行違反 | 2 | 28413948, 853388885 |
| D | Googleルートが通っていない道を違反判定 | 1 | 151808609 |

**本調査の対象は B の7件。**

### 仮説

B の7件は全て `trunk` / `primary` / `tertiary` の幹線道路に該当する。幹線道路はOSM上で上下線が**別way（分離ウェイ）**としてマッピングされ、それぞれに `oneway=yes` が付与されていることが多い。

Google の polyline は道路中心線ではなく実際の走行軌跡に近い座標列であるため、中心線から数メートルずれた位置が **反対車線の way に最近傍マッチ**し、結果として「逆走」と誤判定されている可能性がある。

**すなわち原因は `check_oneway_violation` の判定ロジックではなく、polyline → way のマッチング処理（`external_route_scorer.py`）にあるという仮説である。**

この仮説の真偽を、事実に基づいて確定させることが本調査の目的。

---

## 重要な前提

- **既存の実験データ（`ground_truth.csv`・`google_comparison.csv`・`google_routes_input.csv`）は一切変更しないこと。**
- **`external_route_scorer.py`・`law_checker.py` 等の本体コードも本調査では変更しないこと。** 修正は原因確定後に別タスクで行う。
- 調査用スクリプトは `/tmp` 配下に作成し、結果のみを新規ファイルに記録する。
- 結果は `bicycle-navi/backend/data/investigation_mismatch_B7.md` に記録する。
- 判断や解釈を先回りせず、取得した数値をそのまま提示すること。

---

## Step 1: 違反検出点の座標とマッチしたwayの再現

前回の調査で使用した再現スクリプト（`google_routes_input.csv` の polyline に対して `external_route_scorer.py` の判定ロジックを呼び出すもの）を再度実行し、B の7件それぞれについて以下を抽出する。

**記録項目:**

| 項目 | 説明 |
|---|---|
| `way_id` | マッチした way のID |
| `violation_point_lat` / `violation_point_lng` | 違反と判定された polyline 上の点の座標 |
| `polyline_prev_lat` / `polyline_prev_lng` | その直前の polyline 点の座標 |
| `polyline_next_lat` / `polyline_next_lng` | その直後の polyline 点の座標 |
| `matched_way_node_ids` | マッチした way のノードID列 |
| `confidence` | システムが算出した信頼度スコア |

---

## Step 2: 進行方向ベクトルと way 方向の角度差を算出

仮説が正しければ、polyline の進行方向とマッチした way の方向は **約180度** の関係になるはずである。

各違反点について:

1. **polyline の進行方向ベクトル**を算出する
   - `polyline_prev` → `polyline_next` のベクトル（違反点を挟む前後の点を使う）
2. **マッチした way の方向ベクトル**を算出する
   - way のノード列のうち、違反点に最も近いセグメントの始点→終点ベクトル
   - way の `oneway=yes` はノード列の順方向を意味する
3. 両ベクトルの**角度差**を度数で算出する

**判定の目安:**

| 角度差 | 解釈 |
|---|---|
| 0〜45度 | 順走（違反判定自体がおかしい） |
| 135〜180度 | 逆走判定と整合（誤マッチではなく方向は正しく取れている） |
| 45〜135度 | 直交に近い。マッチ対象そのものが誤っている可能性 |

**注意:** 緯度経度から方向を計算する際、日本の緯度（北緯35度付近）では経度1度あたりの距離が緯度1度あたりの約0.82倍になる。角度算出時は経度差に `cos(latitude)` を掛けて補正すること。

---

## Step 3: 分離ウェイ（反対車線）の存在確認

**これが仮説検証の中核。**

各違反点について、Overpass API で**半径30m以内に存在する全way**を取得する。

```
[out:json][timeout:25];
way(around:30,{lat},{lng})["highway"];
out geom tags;
```

**記録項目:**

| 項目 | 説明 |
|---|---|
| 周辺way一覧 | way_id・highway・oneway・name |
| 逆向きペアの有無 | マッチしたwayと同名（`name`が一致）かつ `oneway=yes` で、**方向が逆向き**のwayが存在するか |
| 各wayと違反点の垂直距離 | 違反点から各wayのセグメントへの最短距離（メートル） |
| マッチしたwayの順位 | 距離順で何番目か |

**期待される結果（仮説が正しい場合）:**

- 同名の逆向き `oneway=yes` way が存在する（＝分離ウェイ）
- 違反点からマッチしたwayまでの距離と、逆向きwayまでの距離が**近接している**（差が数メートル程度）
- 逆向きwayを選んでいれば違反にならなかった

---

## Step 4: 対照群としてのA・C群の同一分析

**仮説の妥当性を確認するため、B群以外にも同じ分析を行う。**

以下の way_id について Step 2・Step 3 を実施する。

- **A群（6件）**: 138533178, 23690216, 142222817, 80835360, 350353685, 325347768
- **C群（2件、真の違反）**: 28413948, 853388885

**確認したいこと:**

- **C群（真の違反）** では分離ウェイが存在しない、または存在しても距離差が大きいはず。もしC群でもB群と同じパターンが出るなら、仮説は成立しない
- **A群（生活道路）** では分離ウェイが存在しないはず（片側1車線の小道のため）

この対照がなければ「B群に分離ウェイがあった」だけでは因果を主張できない。**必ず実施すること。**

---

## Step 5: D群（1件）の別途確認

`way 151808609`（千葉→幕張本郷、コードD）は「Googleルートが通っていない道を違反判定している」とユーザーが判定した。これは別種の問題である可能性が高い。

**記録項目:**

- 違反点の座標と、Google polyline 上で最も近い点との距離
- マッチしたwayと違反点の垂直距離
- 周辺30m以内のway一覧

**この距離が大きい場合、マッチング許容半径の設定が緩すぎる可能性を示す。** `external_route_scorer.py` 内でマッチング半径を定めている定数があれば、その値と該当行番号を記録すること。

---

## 成果物

`bicycle-navi/backend/data/investigation_mismatch_B7.md` に以下の構成で記録:

1. **Step 1 結果**: B群7件の座標・マッチway・信頼度の一覧表
2. **Step 2 結果**: 角度差の一覧表（B群7件）
3. **Step 3 結果**: 各違反点の周辺way一覧と分離ウェイの有無・距離差
4. **Step 4 結果**: A群・C群の同一分析結果（対照）
5. **Step 5 結果**: D群1件の分析
6. **総括表**: 全18件について「分離ウェイの有無 / 角度差 / 距離差 / 現地判定コード」を1枚にまとめた表
7. **結論**: 下記の判定基準に従い、仮説の成否を記述

---

## 判定基準

| B群の状況 | C群の状況 | 結論 |
|---|---|---|
| 分離ウェイあり・距離差小 | 分離ウェイなし | **仮説成立**（マッチング精度の問題） |
| 分離ウェイあり・距離差小 | 分離ウェイあり・距離差小 | **仮説不成立**（分離ウェイの有無では説明できない。別要因） |
| 分離ウェイなし | — | **仮説不成立**（別要因を探す必要あり） |
| 一部のみ分離ウェイあり | — | **部分的成立**（該当件数を明記し、残りは別要因として保留） |

**結論セクションでは推測を書かないこと。** データから断定できない場合は「断定できない。追加で必要な情報は〜」と明記する。

---

## 修正案の提示について

**本タスクでは修正コードを書かないこと。** ただし、仮説が成立した場合に限り、報告書の末尾に以下を**提案として**記述してよい。

- `external_route_scorer.py` のマッチング処理の該当行番号
- 現在の実装が「最近傍1本」を選んでいるか、複数候補から選択しているか
- 修正の方向性（例：候補wayを複数取得し、polyline進行方向との角度差が最小のものを選ぶ）

実装は別タスクとする。
