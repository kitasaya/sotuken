# 卒研: 垂線距離修正後に生じた想定外4地点の原因調査

## 背景・現状

`services/overpass.py` の `get_bulk_way_data` を、ノード距離ベースから
垂線距離ベースの最近傍way選択に修正した（`fix_verification.md` 参照）。

修正の主目的であった **B群（分離ウェイの誤マッチ）6件は全消滅**し、
**C群（真の違反）3件は無影響**、**自システム15ペアは完全一致**という
想定通りの結果が得られた。

しかし以下の**想定外の変化**が生じている。

| # | label | way_id | 現地判定 | 変化 |
|---|---|---|---|---|
| 1 | 品川→東京 | 23690216 | A（自転車を除く） | **消滅**（残るはずだった） |
| 2 | 荻窪→阿佐ヶ谷 | 80835360 | A（自転車を除く） | **消滅**（残るはずだった） |
| 3 | 浦和→さいたま新都心 | 325347768 | A（自転車を除く） | **消滅**（残るはずだった） |
| 4 | 新宿→池袋 | （idx=22 明治通り） | 未検証 | **新規出現**（無かったはず） |

A群は「現地に自転車除外の補助標識があるがOSMに未反映」という
**データ不備**に起因する検出であり、マッチング方式を変えても
`oneway=yes` タグ自体は実在するため**消えないはず**だった。

前回の作業で「交差点近傍・近接する無関係な地物（歩道・交差道路）への
誤マッチ」という説明が示されているが、これは**まだ仮説であり実証されていない**。

**本調査の目的は、この4地点で何が起きているかを事実に基づいて確定させることである。**
修正作業は原因確定まで行わない。

---

## 仮説

### 仮説X：近接地物への誤マッチ

垂線距離を用いると、交差点近傍や歩道が併設された区間において、
本来の車道より**歩道・自転車道・交差道路の線分の方が垂線距離で近くなる**
ケースが生じる。これらの地物には `oneway=yes` が無いため違反が消える
（A群3件の消滅）、あるいは逆に別の `oneway=yes` を持つ地物を拾って
新規違反が出る（新宿→池袋）。

### 仮説Y：同一道路の別セグメントへの乗り換え

同じ道路が複数の way に分割されている場合、垂線距離では隣接する
別 way にマッチする。その way のタグが異なれば判定が変わる。

### 仮説Z：距離計算の実装上の誤り

`_point_to_segment_dist_m` の緯度補正やクランプ処理に誤りがあり、
特定の条件下で不正な距離を返している。

**この3つを切り分けることが本調査のゴール。**

---

## 重要な前提

- **コードは一切変更しない。** 本タスクは調査のみ。
- 既存データ（`google_comparison.csv`・`google_comparison_after_fix.csv`・
  `ground_truth.csv` 等）を変更しないこと。
- 調査用スクリプトは `/tmp` 配下に作成する。
- 結果は `backend/data/investigation_perpendicular_side_effects.md` に記録する。
- 判断や解釈を先回りせず、取得した数値をそのまま提示すること。

---

## Step 1: 4地点の判定点と前後のマッチ結果を特定

`scripts/rerun_r2_after_fix.py` を流用し、4地点それぞれについて以下を抽出する。

| 項目 | 説明 |
|---|---|
| `point_lat` / `point_lng` | 判定点の座標 |
| `sample_idx` | ルート上のサンプル番号 |
| `travel_vector` | その点での進行方向ベクトル |
| **修正前** matched way_id / name / highway / oneway / 距離 | ノード距離ベースでの結果 |
| **修正後** matched way_id / name / highway / oneway / 距離 | 垂線距離ベースでの結果 |

新宿→池袋（idx=22）は修正前に違反でなかったため、
**修正前に何にマッチしていたか**も必ず記録すること。

---

## Step 2: 各地点の周辺地物を全件列挙

各判定点について、半径30m以内の全 way を Overpass で取得する。

```
[out:json][timeout:25];
way(around:30,{lat},{lng})["highway"];
out geom tags;
```

各 way について以下を算出し、**垂線距離の昇順**で表にする。

| way_id | name | highway | oneway | ノード距離(m) | **垂線距離(m)** | 順位(垂線) | 順位(ノード) |
|---|---|---|---|---|---|---|---|

**確認したいこと：**

- 修正後にマッチした way は、垂線距離で本当に1位か
- 修正前にマッチした way（A群の場合は `oneway=yes` を持つ本来の道路）は
  垂線距離で何位か
- 1位に来た way の `highway` は何か（`footway` / `path` / `cycleway` /
  `service` など、車道以外か）

**仮説Xが正しければ：** 修正後にマッチした way は `highway=footway` や
`cycleway`、あるいは交差する別名の道路であるはず。

**仮説Yが正しければ：** 修正後にマッチした way は、修正前と `name` が同じで
way_id だけが異なるはず。

---

## Step 3: 距離計算の妥当性検証（仮説Zの検定）

Step 2 で算出した垂線距離が正しいことを、独立した手段で検証する。

各地点について、修正後にマッチした way と修正前にマッチした way の2本を対象に：

1. `services/overpass.py` の `_point_to_segment_dist_m` で計算した値
2. **独立実装**（`geopy.distance` または haversine を用いた別式）で計算した値

を比較する。両者が一致しなければ仮説Zが疑われる。

**特に確認すべき点：**

- 緯度補正の係数（`111_320 * cos(lat)` と `110_540`）が妥当か
- クランプ処理（射影が線分外に落ちる場合）が正しく働いているか
- 判定点が線分の端点近傍にある場合の挙動

**簡易な検算方法：** 座標を直接 Google Maps 等の距離計測で確認できる地点が
あれば、実測値と突き合わせる。

---

## Step 4: 対照群の確認（重要）

**この修正で「正しく動いた」地点でも同じ分析を行い、比較する。**

対照として以下を分析する：

- **B群で正しく消えた4点**（外苑東通り・中央通り・南武沿線道路・国道126号）
- **C群3点**（28413948 ×2、853388885）
- **A群で残った3件**（消滅しなかった方の3件。way_idは
  `google_comparison_after_fix.csv` から特定すること）

**確認したいこと：**

- 正しく動いた地点では、垂線距離1位の way が「車道かつ想定通りの道路」に
  なっているか
- 消えたA群3件と残ったA群3件で、周辺地物の構成に差があるか
  （例：消えた方だけ歩道が併設されている、交差点に近い、など）

**この対照がなければ「4地点に歩道があった」だけでは因果を主張できない。
必ず実施すること。**

---

## Step 5: 交差点からの距離の測定

仮説Xが「交差点近傍で起きる」と述べているため、これを数値で確認する。

各判定点について、最寄りの交差点（3本以上の way が共有するノード）までの
距離を算出する。

```
[out:json][timeout:25];
node(around:50,{lat},{lng});
out;
```
から、複数 way に属するノードを抽出するか、
または Overpass の `way(around:...)` の geometry を突き合わせて共有ノードを求める。

**比較対象：**

| 群 | 交差点までの距離（中央値） |
|---|---|
| 想定外4地点 | ? |
| 正しく動いたB群4点 | ? |
| C群3点 | ? |
| 残ったA群3件 | ? |

想定外の地点が交差点に有意に近ければ、仮説Xの「交差点近傍」という
条件が裏づけられる。

---

## 成果物

`backend/data/investigation_perpendicular_side_effects.md` に以下を記録：

1. **Step 1 結果**：4地点の修正前後マッチ比較表
2. **Step 2 結果**：各地点の周辺地物一覧（垂線距離順、highway・oneway 付き）
3. **Step 3 結果**：距離計算の独立検証結果（仮説Zの成否）
4. **Step 4 結果**：対照群（B群・C群・残存A群）の同一分析
5. **Step 5 結果**：交差点からの距離の群間比較
6. **総括表**：全地点について「修正前way / 修正後way / highway種別 /
   交差点距離 / 現地判定コード」を1枚にまとめる
7. **結論**：下記の判定基準に従い、どの仮説が成立するかを記述

---

## 判定基準

| 修正後マッチのhighway | 修正前との name 一致 | 距離計算の独立検証 | 結論 |
|---|---|---|---|
| `footway`/`path`/`cycleway` 等の非車道 | — | 一致 | **仮説X成立**（近接地物への誤マッチ） |
| 車道だが別名 | 不一致 | 一致 | **仮説X成立**（交差道路への誤マッチ） |
| 車道で同名・別way_id | 一致 | 一致 | **仮説Y成立**（同一道路の別セグメント） |
| — | — | **不一致** | **仮説Z成立**（実装の誤り。最優先で修正） |
| 上記に当てはまらない | — | 一致 | **いずれも不成立**。別要因を記述 |

複数地点で異なる仮説が成立する場合は、**地点ごとに結論を分けて記述する**こと。
無理に単一の原因にまとめないこと。

**結論セクションでは推測を書かないこと。** データから断定できない場合は
「断定できない。追加で必要な情報は〜」と明記する。

---

## 修正案の提示について

**本タスクでは修正コードを書かないこと。** ただし、原因が確定した場合に限り、
報告書の末尾に以下を**提案として**記述してよい。

- 想定される修正の方向性（例：マッチ候補から特定の `highway` 値を除外する、
  進行方向との角度差を選択基準に加える、等）
- その修正が B群・C群・A群それぞれに与えうる影響の見込み
- 副作用のリスク

実装は別タスクとする。

---

## 補足：現地確認との関係

本調査の後、想定外4地点についてユーザーがストリートビューで現地確認を行う。
その際に確認すべき項目を、調査結果を踏まえて報告書末尾に列挙すること。

例：「この地点は歩道が併設されているか」「交差点の形状はどうなっているか」
「修正後にマッチした way は現地でどの地物に相当するか」など、
**現地で目視確認可能な形の質問**として記述すること。
